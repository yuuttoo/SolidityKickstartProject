var Routes = (function () {
    function Routes(routes) {
        this.routes = Object.keys(routes).map(function (key) { return routes[key]; });
    }
    Routes.prototype.getRoute = function (url) {
        return this.routes.filter(function (route) { return route.isMatch(url); })[0];
    };
    return Routes;
}());
export { Routes };
