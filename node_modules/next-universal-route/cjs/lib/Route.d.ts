import { NextRoute } from './NextRoute';
export declare class Route {
    path: string;
    page?: string;
    private _query;
    private readonly urlFormatter?;
    private readonly params;
    private queryStringParams;
    private subdomains;
    private readonly customHandler;
    constructor(path: string, page?: string, urlFormatter?: Function, customHandler?: Function);
    get query(): object;
    generateUrl(params?: object, queryStringParams?: object, options?: {
        encode: boolean;
    }): NextRoute;
    generateFromUrl(url: string, params: object): NextRoute;
    isMatch(url: string): boolean;
    hasSubdomain(subdomain: string): boolean;
    addSubdomain(subdomain: string): Route;
    getCustomHandler(app: Function): Function;
    private setPage;
    private formatUrl;
    private getQuery;
    private static parseUrl;
}
